[project]
name = "estoque-django"
version = "0.1.0"
description = "MVP de micro SaaS de controle de estoque com Django"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "django>=5.1,<6",
    "psycopg[binary]>=3.2,<4",
]
